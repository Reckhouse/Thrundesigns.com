import { defineCliConfig } from "sanity/cli";

export default defineCliConfig({
  api: {
    projectId: "fbuy6kak",
    dataset: "production",
  },
  studioHost: "thrundesign",
  autoUpdates: true,
});
